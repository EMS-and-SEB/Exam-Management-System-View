/** sessionStorage keys for student exam persistence. Kept in their own module
 * to avoid a circular import between the session store and the exam store. */
export const SESSION_STORAGE_KEY = "ems-student-session";
export const EXAM_STORAGE_KEY = "ems-exam";