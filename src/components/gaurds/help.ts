/**
 * Guards Folder
 * 
 * Contains route guards and authentication middleware including:
 * - PermissionGuard for role-based access control
 * - AuthGuard for authorization checks

 * Usage: Wrapped around protected routes to enforce access policies
 */
