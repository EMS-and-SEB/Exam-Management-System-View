/**
 * Cohorts Module
 * 
 * Manages cohort/batch data including:
 * - API calls for cohort CRUD operations
 * - Cohort schemas and type definitions
 * - Cohort-related hooks and state management
 * - Cohort components (list, detail, forms)
 * - Cohort pages (if necessary)
 * 
 * Shared: Uses src/lib utilities, src/components UI elements
 */
