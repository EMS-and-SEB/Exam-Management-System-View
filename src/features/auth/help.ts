/**
 * Auth Module
 * 
 * Handles authentication and authorization logic including:
 * - User login/logout and session management
 * - API calls for authentication endpoints
 * - Auth schemas and validation types
 * - Custom hooks for auth state and operations
 * - Auth components (login forms, auth guards)
 * - Auth pages (if necessary)
 * 
 * Shared: Uses src/lib for utilities, src/components for UI
 */
