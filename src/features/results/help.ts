/**
 * Results Module
 * 
 * Handles exam results and performance analytics including:
 * - API calls for results retrieval and analysis
 * - Result schemas and score calculation types
 * - Result hooks for aggregation and filtering
 * - Result components (score cards, performance charts)
 * - Result pages (if necessary)
 * 
 * Shared: Uses src/lib for calculations, src/components for charts and UI
 */
